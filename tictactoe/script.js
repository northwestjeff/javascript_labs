// Set up HTMl CSS JS Git
// store gameboard inside array of gameboard object
// players stored in objects
// gameplay controller stored in objects

// if you need ONE: module.  TWO: Factories

// JS should render gameboard contents

//build functions that allow players to add marks
// no selecting spots already filled

// Each piece of functionality should fit into game, player or gameboard

// check for 3 in a row or tie

// Clean up interface to add name, restart


function gameBoard(cols, row) {
    for (i=0; i<cols)
}